배열
- 여러개의 데이터를 담을 수 있는 자료구조
- 배열 안에 들어있는 데이터를 요소라고 한다.
- index라고 하는 요소들의 위치번호를 가지고 있다.(0부터 1씩 순차적으로 증가)
- 데이터를 한 곳으로 모아서 관리하기 편하다.

객체
현실세계에 존재하는 유형, 무형의 것을 디지털화 해서 코드로 표현

객체의 생성
객체 리터럴 방식 {프로퍼티 작성}
객체에 데이터는 키:값이 한쌍으로 이루어져 저장이 된다.

객체의 값에 접근하는 방법
변수명.키
ㄴ 프로퍼티에 들어있는 값을 출력, 수정하는것이 가능하다.

객체에 프로퍼티를 추가하는 방법
변수명.새로운키 = 값;
객체에 프로퍼티가 없었다면 추가가된다.

delete 변수명.키

객체는 속성(상태)를 저장할 수 있고, 행동(행위)도 함수로 표현할 수 있다.

객체 안에 정의된 함수 -> 메서드

함수 : 독립적으로 호출이 가능하다.

let arr = [1,2,3,4,5]

arr.filter()
arr.map()
arr.reduce()
arr.push()
arr.pop()

let car = {

    drive : function(){ -> 메서드 정의
	    기능
	}
}

car.drive(); -> 메서드의 호출

DOM(Document Object Model)
브라우저가 HTML코드를 읽어서 만든 트리구조의 데이터이다.

부모,자식,형제 관계가 존재한다.

document객체
웹페이지 전체를 대표하는 최상위 객체

document 메서드로 HTML 태그 선택하기

document.querySelector(css선택자)

js를 이용하여
속성을 조정
스타일을 조정

HTML에서 버튼을 클릭하면 <p>태그의 내용을
"버튼이 클릭되었습니다"라고 바꾸는 코드 작성하기

<button id=btn>클릭</button>
<p id="message">아직 클릭하지 않았습니다.</p>

textContent = 


















